    // Code generated by xorm reverse.
    // 表结构函数
    package dbmodel

    {{range .Tables}}
    type t{{TableMapper .Name}} struct {
    {{range .Columns}} {{.FieldName}} string
    {{end}}
    }
    var T{{TableMapper .Name}} = &t{{TableMapper .Name}} {
      {{range .Columns}} {{.FieldName}}: "{{.Name}}",
      {{end}}
    }
    {{end}}