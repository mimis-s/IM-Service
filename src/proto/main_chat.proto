syntax = "proto3";

package im_main_proto;

// 生成golang文件的目录
option go_package = "/im_main_proto";

message ChatSingleReq {
    string TestStr = 1;
}

message ChatSingleRes {
    string TestStr = 1;
}